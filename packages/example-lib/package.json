{
  "name": "example-lib",
  "private": true,
  "version": "0.3.5",
  "main": "./src/index.js",
  "devDependencies": {
    "doug-lib": "^0.3.5"
  },
  "scripts": {
    "build": "doug-lib build",
    "test": "doug-lib build && doug-lib test",
    "release": "doug-lib release",
    "shell": "doug-lib shell"
  }
}
