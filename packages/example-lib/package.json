{
  "name": "example-lib",
  "version": "0.0.1",
  "main": "./src/index.js",
  "devDependencies": {
    "doug-lib": "^0.0.1"
  },
  "scripts": {
    "build": "doug-lib build",
    "test": "doug-lib build && doug-lib test",
    "release": "doug-lib release"
  }
}
