'use strict'

const shelljs = require('shelljs')
const write = require('../utils/write')

module.exports = {
  options: (program) => {
    return program
  },
  action: (config, options) => {

  },
}
