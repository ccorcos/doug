{
  "name": "example-app",
  "version": "0.1.0",
  "devDependencies": {
    "doug-app": "^0.1.0"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "doug-app dev",
    "build": "doug-app build",
    "deploy": "doug-app deploy",
    "test": "doug-app test",
    "release": "doug-app release"
  }
}
