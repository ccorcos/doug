{
  "name": "example-app",
  "private": true,
  "version": "0.3.2",
  "devDependencies": {
    "doug-app": "^0.3.2"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "doug-app dev",
    "build": "doug-app build",
    "deploy": "doug-app deploy",
    "test": "doug-app test",
    "release": "doug-app release",
    "shell": "doug-app shell"
  }
}
