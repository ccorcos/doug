{
  "name": "example-app",
  "version": "0.0.1",
  "devDependencies": {
    "doug-app": "^0.0.1"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "doug-app dev",
    "build": "doug-app build",
    "deploy": "doug-app deploy",
    "test": "doug-app test",
    "release": "doug-app release"
  }
}
